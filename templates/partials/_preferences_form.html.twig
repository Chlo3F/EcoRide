
<form id="preferences-form" data-url="{{ path('submit_preferences') }}">
    {{ form_widget(preferencesForm) }}
    <button type="submit" class="btn btn-primary mt-2">Sauvegarder préférences</button>
</form>
